import * as React from 'react'
import { JournalEntry } from '../../types'

interface JournalEntryLinkProps {
  entry: JournalEntry
  /* */
}

export const JournalEntryLink = (props: JournalEntryLinkProps) => {
  return <div>...</div>
}
